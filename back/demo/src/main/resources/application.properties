spring.application.name=controle_financeiro


spring.datasource.url=jdbc:mysql://localhost:3306/controle_financeiro
spring.datasource.username=controlador
spring.datasource.password=senha123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Flyway: when the schema is non-empty but no schema history table exists,
# initialize the schema history table instead of failing.
spring.flyway.baseline-on-migrate=true
